name: warm-keeper

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: HEAD-ping HF Space
        run: curl -sfI "${{ vars.HF_SPACE_URL }}" || true
