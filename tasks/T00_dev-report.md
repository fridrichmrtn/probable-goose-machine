# /dev Report

**Task:** T00 — Project bootstrap (pyproject, dirs, deps, app.py stub)
**Branch:** dev/t00-project-bootstrap
**Worktree:** /home/mf/GitHub/probable-goose-machine/.worktrees/t00-project-bootstrap
**Stack:** py, gradio (post-implementation; greenfield at Phase 0)

## Files touched
- `pyproject.toml` — project metadata, ruff/mypy/pytest config, runtime + dev deps
- `uv.lock` — generated by `uv add`
- `src/gander/__init__.py` — `__version__ = "0.1.0"`
- `src/gander/prompts/.gitkeep` — placeholder
- `tests/__init__.py` — empty
- `tests/fixtures/cvs/.gitkeep` — placeholder
- `scripts/.gitkeep` — placeholder
- `eval_outputs/.gitkeep` — placeholder
- `.env.example` — MINIMAX_API_KEY, optional ANTHROPIC_API_KEY, GANDER_MODEL_PROFILE
- `README.md` — HF Space frontmatter + bootstrap stub
- `app.py` — minimal Gradio Blocks placeholder
- `tasks/T00_bootstrap.md` — Status → done, Outcome filled
- `tasks/todo.md` — T00 line checked
- `tasks/dev-plan.md` — implementation plan (174 lines)

## Checks
| Command | Initial | After heal |
|---|---|---|
| `uv sync` | pass | n-a |
| `uv run python -c "import gander; print(gander.__version__)"` | pass | n-a |
| `uv run python -c "import app"` | pass | n-a |
| `uv run pytest --collect-only` | pass (0 tests) | n-a |
| `uv run ruff check .` | pass | n-a |
| `uv run ruff format --check .` | pass | n-a |
| `uv run mypy src/gander` | pass | n-a |

No heal needed — no failing checks, no real must-fix, hiring grade ON-BAR.

## Review findings

### Must-fix (resolved this run — false positive)
- [product-owner] `pyproject.toml:54` — claimed `mypy>=2.0.0` would not resolve. Verified false: Phase 2 implementation showed mypy 2.0.0 resolved successfully and `uv run mypy src/gander` exited 0. mypy 2.0 was released; the finding was based on stale knowledge. No action needed.

### Must-fix (remaining — exhaustion)
None.

### Should-fix (deferred)
- [ux-engineer] `README.md:3-5` — HF Space frontmatter (`📄` / `colorFrom: indigo` / `colorTo: purple`) drifts from `tasks/T22_deploy.md` spec (`📊` / `blue` / `indigo`). T22 will silently overwrite or someone will reconcile late. Worth aligning to T22 spec now (4-line edit) so deploy is a no-op on metadata.
- [product-owner, ux-engineer] `app.py:5` — Placeholder Markdown leaks internal task IDs ("T15 / T16") and a repo path (`tasks/PLAN.md`) into the rendered Gradio surface. Reviewer-facing tone mismatch vs. PRD §9 ("senior submission"). Replace with neutral copy ("Coming soon — full pipeline lands shortly.").
- [ux-engineer] `README.md:13` — Stub references `tasks/T23_readme.md` internally. Same tone issue. Add a one-line "What this is" sentence so the README is never embarrassing in isolation.
- [hiring-manager] `pyproject.toml:5` — `requires-python = ">=3.11"` allows local resolution to 3.12 (and the venv was provisioned with 3.12), while HF Spaces is pinned to 3.11. Dev/prod skew is a senior-bar tell. Tighten to `~=3.11.0` and commit `.python-version` so local matches deploy.
- [hiring-manager] `pyproject.toml` — `pre-commit` is in dev deps but no `.pre-commit-config.yaml` ships in T00. T03 explicitly owns the config, but coupling the dep with its config in the same task is cleaner. Reasonable to defer; flag for T03.
- [hiring-manager] `pyproject.toml:38-41` — `[tool.mypy]` strict only on `src/gander`; tests entirely excluded from type checking. Defensible cut for a senior submission, but undocumented. Either widen to `tests/` (non-strict) or note the cut.

### Nits
- [ai-ml-engineer] `pyproject.toml:11` — Pydantic floor `>=2` is loose; T02 uses v2.7+ JSON-mode patterns. Tighten to `>=2.7`.
- [ai-ml-engineer] `.env.example:2` — `ANTHROPIC_API_KEY` commented hint without `anthropic` package; correct per T05 scope, but T05 owner must remember to `uv add anthropic` before flipping the provider.
- [product-owner] `.gitignore:35` — `eval_outputs/*.md` is narrower than spec's `eval_outputs/*`. Probably deliberate (allows committing JSON/CSV eval artifacts), but undocumented.
- [hiring-manager] `app.py:9` — `demo.launch()` with no args is fine locally; HF Spaces typically wants `server_name="0.0.0.0"`. T16 owns the UI; defer.
- count: 5 (listed above; not blocking)

## Hiring grade
**ON-BAR** — competent, contract-faithful, restrained scaffolding. Python 3.11/3.12 venv drift and missing pre-commit config kept it from STRONG.

## What this revealed about the author (hiring-manager)
> Deliberate, contract-driven. The pyproject is tight (markers documented with rationale, ruff/mypy/pytest all present, dev group separated), the directory layout matches spec exactly, `.gitignore` is thoughtful (`!eval_outputs/.gitkeep` shows they actually thought about the regenerated-output pattern), and the app stub is the *correct* minimal — a Blocks with a "not yet wired" Markdown rather than a literal empty `gr.Blocks()` that would render blank to a reviewer who clicks too early. README defers to T23 instead of bloating. That's senior judgment.

## Cleanup
When you're done with this work:
```
git worktree remove .worktrees/t00-project-bootstrap
git branch -D dev/t00-project-bootstrap
```
Or, to land it:
```
git checkout main
git merge --no-ff dev/t00-project-bootstrap
```
