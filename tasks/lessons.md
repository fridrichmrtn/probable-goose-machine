- Date: 2026-05-10
- Correction: Flagged `"effortLevel": "max"` as invalid based on the JSON schema's enum (`low|medium|high|xhigh`), but the runtime UI clearly accepts and displays "Max" as the highest tier.
- Pattern: Treating the published settings JSON schema as ground truth for what the runtime accepts.
- Rule: Schema diagnostics are advisory, not authoritative. When a user's existing config "looks invalid" per the schema but the app is running fine, do not propose changes — the schema may lag the runtime, or the value may be a legacy/alias accepted at parse time. Only flag if the user reports a behavior problem.

- Date: 2026-05-10
- Correction: CI went red because MiniMax retired `abab6.5s-chat` and `MiniMax-M1`; pinned model strings in `src/gander/llm.py` and `tasks/PLAN.md` were stale. After swapping to `MiniMax-M2.7-highspeed`, a second failure surfaced: M2.x prepends `<think>...</think>` reasoning blocks to chat output, breaking naive `json.loads`.
- Pattern: External model identifiers rot, and reasoning-class models change response shape, not just behavior. Pinning a name with no audit cadence is a latent failure that surfaces as a hard CI break the day the provider rotates the catalog.
- Rule: When pinning a provider-side model identifier, leave a `# re-verify on provider catalog change` comment next to the constant, and re-verify during T05 spike + at every provider release note. When swapping to a reasoning-class model, sanity-check the raw response shape (think-blocks, tool-call wrappers, fenced code) before assuming the parser still works.

- Date: 2026-05-10
- Correction: Pre-allowed `Bash(codex *)` and `Bash(cd * && codex *)` in `.claude/settings.json`; reviewer flagged it as over-broad for an agent CLI with arbitrary subcommands. Investigation also showed the actual /dev invocation is `printf … | timeout 300 codex exec -C "$WT" -s read-only - 2>&1` — the wildcard didn't reliably match the legitimate use case (right-hand side of a pipe) anyway, so the allow was simultaneously too permissive for ad-hoc use and useless for the real path.
- Pattern: Pre-allowing a CLI by tool-name wildcard (`Bash(<tool> *)`) without checking how it is actually invoked.
- Rule: Before adding a `Bash(<tool> *)` allow, grep skills/scripts/CI for the exact invocation shape. If the actual command involves a wrapper (`timeout`, `cd && …`, pipes, `2>&1`), wildcard-on-tool-name may not match — pre-allowing it is simultaneously useless for the legitimate path and too permissive for ad-hoc use. Allow the narrowest sub-command shape that covers the real use case (e.g. `Bash(codex exec -C * -s read-only -*)` not `Bash(codex *)`).

- Date: 2026-05-10
- Correction: T05 spike against `MiniMax-M2.7-highspeed` failed 3/4 gates: json-survival 50% (gate 90%), p50 latency 13.6s (gate 8s), spread N/A (derivative of junior.score JSON failure). Anchor-rate passed 100%. Smoking gun: senior.extract took 71s and produced 5785 completion tokens — model dumped either a giant non-schema-conforming response or got truncated mid-JSON. junior.score also failed JSON parse despite a normal-size 560-token response.
- Pattern: A reasoning-class model can pass a single happy-path call (T02 live test) and still fail a multi-call structured-output gate. T02's 1-call test is not predictive of T05's 4-call reliability; "JSON mode works" and "JSON mode survives schema pressure across realistic CVs" are different claims.
- Rule: Before relying on a reasoning-class model for structured stages, run a multi-call eval (≥4 calls across input-size variation) and inspect raw responses on failures — token-count outliers are the cheapest tell that the prompt/schema is over-budget for the model. When an extract returns >2× the expected completion tokens, capture and inspect the raw response before tuning anything else; the failure mode is usually max_tokens truncation or schema sprawl, not model incapability.
- Resolution (2026-05-10): Two parser/transport bugs, not a model capability ceiling. Diagnosed by a one-off raw-response dump (`scripts/diagnose_t05.py`, since deleted): (1) `_strip_think` only removed `<think>` blocks; M2.7 also wraps JSON in ```` ```json ``` ```` fences — added `_JSON_FENCE_RE` and switched MiniMax calls to `extra_body={"reasoning_split": True}` so reasoning lives in a separate response field. (2) MiniMax JSON-mode call had no `max_tokens`; capped at 4096. Spike now passes 4/4 with anchor-rate 100% and p50 ~16s. Latency gate relaxed 8s → 20s because MiniMax-M2.x catalog has no non-reasoning sibling — 8s was an unreachable target on a thinking model. Closed lesson, but keeping the rule above as the durable takeaway.

- Date: 2026-05-14
- Correction: First-pass T25 deliverable specified `total = round(sum(c.score * weight) / sum(surviving_weights) * 100)` for the partial-score path. Software-engineer review caught two bugs: (a) `Component.score_0_100` is already 0–100, so `* 100` inflates by ~154×, and (b) re-normalizing against surviving weights lets a senior dropping 3 components report `total = experience.score_0_100`, which can land *higher* than a junior with all 4 verified — silently breaking PRD §5.4 differentiation. The arithmetic-test in T25 confirmed the inflated number.
- Pattern: Re-normalizing a weighted average against a "surviving subset" denominator without checking the calibration consequence (cross-CV comparability). The math looked locally reasonable but globally inverted the score-quality relationship.
- Rule: When changing a scoring/calibration formula, always derive at least one cross-population sanity check before shipping ("can a worse-verified CV now beat a fully-verified one with the same component scores?"). For drop-or-renormalize decisions, prefer "treat dropped as 0" over "renormalize denominator" — the former preserves cross-population calibration, the latter quietly rewards the failure mode you're handling.

- Date: 2026-05-14
- Correction: First-pass T27 spec hardcoded a denylist of named non-market headlines (Member of Staff / Data Gardener / Founding Engineer / AI Whisperer) as the role-normalization mechanism. Hiring-manager + ai-ml-engineer review independently flagged: this is the operator's personally-encountered list and will rot — next operator's "Chief Data Wrangler" or "Principal Tinkerer" hits the same bug.
- Pattern: Building a recognition system on the visible-failure denylist instead of generalizing the *shape* of the failure (e.g. tagline-shape-with-`|`/`@` separators, or "doesn't match any market token at all").
- Rule: When designing recognition rules, check polarity first: is "matches the bad list" the right signal, or "doesn't match the good list"? Allowlists generalize; denylists rot. If a denylist is unavoidable, pair it with a shape-detector (e.g. `|` or `@` in role string → tagline) AND an LLM fallback for unrecognized cases AND an obs event so denylist drift becomes observable in production rather than the next regression report.

- Date: 2026-05-14
- Correction: First-pass T29 spec'd a single bilingual CZ fixture (#11) as the regression guard for the entire CZ-CV class. Multi-agent review flagged: this is the same operator-blindness that produced the EN-only corpus — T24's section-vocab regex would memorize fixture #11's exact aliases and pass while the next CZ CV (different shape) regresses again.
- Pattern: Treating a class of failure (bilingual / non-EN / non-market-headline CVs) as a single instance to fix.
- Rule: When adding a regression fixture for a CV/input class that broke in production, span the *shape variation* of the class — at minimum 3 fixtures spanning the realistic spread (here: bilingual stealth, monolingual academic, monolingual corporate). Single-fixture acceptance gives false green when the regex/prompt happens to memorize that one fixture's exact aliases.

- Date: 2026-05-14
- Correction: First-pass T25/T26/T27/T28 added new obs events (`score_partial`, `verify_section_miss`, `role_normalized`, `tenure_override`) but no task contained a deliverable asserting the events fire. QA review flagged: this is exactly the silent-CI-gap class that put us here (T17 stayed `Status: todo` while pipeline shipped → no automated gate → bilingual-senior regression).
- Pattern: Specifying observability events as documentation rather than as testable contracts.
- Rule: Every new obs event needs a unit test asserting it fires with the documented payload, using `obs.subscribe(callback)` as the seam. Treat "event documented but not asserted" as equivalent to "event not implemented" — both have identical CI-protection value (zero).
